pipeline {
    agent any

    environment {
        VENV_DIR = 'venv'
    }

    stages {
        stage('Checkout') {
            steps {
                sh 'git clone https://github.com/dawidziel/szaszki-chess.git'
                sh 'mkdir -p test_szachy && cd test_szachy && git checkout main'
            }
        }

        stage('Set up Python') {
            steps {
                sh 'apt-get update'
                sh 'apt-get install -y python3 python3-venv'
                sh "python3 -m venv ${VENV_DIR}"
                sh ". ${VENV_DIR}/bin/activate && python -m pip install --upgrade pip"
            }
        }

        stage('Install Dependencies') {
            steps {
                sh ". ${VENV_DIR}/bin/activate && pip install -r test_szachy/requirements.txt"
            }
        }

        stage('Run Unit Tests') {
            steps {
                sh ". ${VENV_DIR}/bin/activate && pytest test_szachy"
            }
        }
    }

    post {
        always {
            junit '**/test-results.xml'
        }
    }
}